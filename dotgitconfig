[color]
  ui = true
[color "branch"]
  current = yellow reverse
  local = yellow
  remote = green
[color "diff"]
  meta = yellow bold
  frag = magenta bold
  old = red bold
  new = green bold
[color "status"]
  added = yellow
  changed = green
  untracked = red
[user]
	name = Devinder Khroad
	email = dkhroad@gmail.com
	initials = dk
[alias]
	st = status
	di = diff
	co = checkout
	ci = commit
	br = branch
  sta = stash
  pl = pull --ff-only
  llog = log --date=local
    l = log --pretty=oneline --abbrev-commit --graph --decorate
   # l = log --pretty=format:'%h %s (%an)' --abbrev-commit --graph --decorate
  # Other useful aliases:
  unstage = reset HEAD
  staged = diff --cached
  unstaged = diff
[apply]
	whitespace = warn
[branch]
	autosetupmerge = true
[core]
  excludesfile = /Users/dkhroad/.gitignore
  #gitproxy = gitproxy
	autocrlf = input
	editor = vi
	pager = less -FRSX
[push]
  #only push branches that have been set up to tracka remote branch
  default = current
[status]
  submodule=1
[help]
  autocorrect=1
[mergetool "sublime"]
	cmd = subl -w $MERGED
	trustExitCode = false
[merge]
	tool = sublime
[diff]
    #external = /home/dkhroad/bin/diff.py
[diff]
	tool = meld
